[providers]
python = "3.10"

[phases.setup]
nixPkgs = ["postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python -m uvicorn --app-dir backend app.main:app --host 0.0.0.0 --port $PORT"
